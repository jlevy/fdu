---
type: is
id: is-01m0prhqd27m471dn47yt973k0
title: Reference embedder example and cross-engine agreement fixture
kind: task
status: open
priority: 1
version: 7
spec_path: docs/project/specs/active/plan-2026-08-23-fdu-interactive-client-implementation.md
labels: []
dependencies: []
parent_id: is-01m0prgbradma67z3j1wfyh8r7
created_at: 2026-08-23T07:32:19.490Z
updated_at: 2026-08-25T06:30:23.453Z
closed_at: null
close_reason: null
resolution: null
duplicate_of: null
---
A runnable, CI-tested example under crates/fdu-py/examples: boot, serve dual-plane tallies, stream changes with dirty sets, resume from a cursor — the seam of the metabrowser adoption. Plus one committed fixture comparing fdu against the metabrowser walker's semantics (symlinks as leaves, hidden allowlist, gitignore negations), differences documented or eliminated. This is the acceptance test fdu-p02b asked for.

## Notes

Becomes a three-oracle stack per the reconciliation: the vendored File Rollup conformance packet, recorded-observation replay driven into both engines, and filesystem scenarios over immutable or stepwise-mutated fixtures. Running two live engines against one changing tree is explicitly NOT an oracle -- the observation moments are incomparable and the dual walk perturbs what is being measured. Parity fixtures must separately prove that navigation tallies, literal filters, recent rows, and catalog rows retain the RAW logical extension even where classification uses the canonical suffix.

METABROWSER CONTRACT FOLLOW-UP at 68eeaac (2026-08-24). The acceptance adapter exposes one EngineVersion.semantic_fingerprint. FDU may keep its native type, tag, and reducers fingerprints as separate named components; the thin adapter must combine them using MetaBrowser's canonical sorted-name JSON array plus SHA-256 algorithm. The three-oracle acceptance test must assert that changing any component changes the combined identity and that identical components are stable. Its paged recency slice must also reuse one caller-chosen as_of across the pinned assembly.

Reopened: Reopened at exact PR #47 head 4eac1b2. The new reference embedder cannot reproduce MetaBrowser #74. Its semantic_fingerprint serializes JSON objects with integer values and includes hidden admission in the semantic digest, while the MetaBrowser authority requires sorted [name, value] string pairs and separates scope from semantic identity. Its listing accepts an unbounded limit and discards continuation/remainder. Its recent_page passes after and limit to children_of root, not to the FILES report, omits expected version pinning, and returns no recent-row cursor or exact remainder; the test never requests page two. Its invalidation adapter materializes batch.changes and omits the complete resulting provider state and work required by ChangeBatch. Keep the acceptance bead open until the example consumes the thin-provider surface and passes the MetaBrowser provider harness; underlying paging and watch gaps remain fdu-91ru and fdu-vfx7.
