# Command reference

`acorn PATH` prints a directory overview. Selection flags shape the retained answer,
view flags choose its organization, and format flags control serialization. A path is
required so an accidental bare command cannot begin an expensive scan.

Machine formats carry schema, source, freshness, completeness, and timestamps.
